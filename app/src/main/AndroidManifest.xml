<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devahoy.learn30androidlibraries" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".day21.AAApplication">

        <activity
            android:name=".day27.AdapterKitActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<activity android:name=".day2.ParallaxBackgroundActivity" />-->

<!--        <meta-data
            android:name="com.crashlytics.ApiKey"
            android:value="YOUR_API_KEY_FROM_CRASHLYTICS_PLUGIN"/>-->

        <meta-data android:name="AA_DB_NAME" android:value="Devahoy-AA.db" />
        <meta-data android:name="AA_DB_VERSION" android:value="1" />

    </application>

</manifest>
